<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.appsnipp.modernlogin">
    <!-- permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
    tools:replace="android:icon"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- activitys -->
        <activity
            android:name=".Activities.StartActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ActivityRecord"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ActivityForgotPass"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ActivityEmailSent"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- android:windowSoftInputMode="adjustPan" -->

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.SalesReportActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".Fragments.SettingsFragment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Fragments.ReportFragment"
            android:theme="@style/AppTheme.NoActionBar" />


        <activity android:name=".Activities.SettingsActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".Activities.MainExpenseActivity">
            <!-- Parent activity meta-data to support Android 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.MainExpenseActivity"/>
        </activity>

        <activity android:name=".Activities.CategoryEditActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateVisible"
            android:parentActivityName=".Activities.MainExpenseActivity">
            <!-- Parent activity meta-data to support Android 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.MainExpenseActivity"/>
        </activity>

        <activity android:name=".Activities.ExpenseEditActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateVisible"
            android:parentActivityName=".Activities.MainExpenseActivity">
            <!-- Parent activity meta-data to support Android 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.MainExpenseActivity"/>
        </activity>

        <provider
            android:authorities="com.appsnipp.modernlogin.provider"
            android:name=".providers.ExpensesProvider"
            android:grantUriPermissions="true"
            android:exported="false">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
    </provider>

    </application>

</manifest>